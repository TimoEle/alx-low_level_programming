#include "main.h"

/**
 * _atoi -Entry point
 *
 * Description: Converts a string to an integer
 * @s: pointer
 *
 * Return: Integer
 */

int _atoi(char *s)
{
	if (*s != '\0')
	{
		return (0);
	}
	return (1);
}
